interface BinModel {
    id: number;
    code: string;
    whId: number,
    zoneId: number,
    zoneCode: string,
    dutyId: number,
    dutyCode: string,
    sizeCode: string,
    locateCode: string,
    typeCode: string,
    comment: string,
}